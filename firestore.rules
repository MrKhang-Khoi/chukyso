rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Quy tắc bảo mật Cloud Firestore cho hệ thống ký số giáo án
    // Cho phép đọc/ghi an toàn trong nội bộ trường học
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
